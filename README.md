Backend-приложение для управления делами (Cases) с поддержкой статусной машины, истории изменений и автоматических процессов.


🔧 Запуск проекта
Требования

Java 

Maven 

PostgreSQL

Spring Boot

⚙️ Переменные окружения

Пример application.yml:

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/case_db
    username: root
    password: test1234

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

🚀 Запуск
mvn clean install
mvn spring-boot:run


Приложение будет доступно:

http://localhost:8089


Swagger UI:

http://localhost:8089/swagger-ui.html

🏗 Архитектура

Проект построен по слоистой архитектуре:

controller → service → repository → database

Слои приложения
Controller

Обрабатывает HTTP-запросы

Использует DTO

Не содержит бизнес-логики (тонкий слой)

Service

Содержит бизнес-логику

Управляет статусами

Валидирует переходы

Записывает историю изменений

Работает в транзакциях

Repository

Доступ к данным через Spring Data JPA

Изолирован от бизнес-логики

Entity

JPA-сущности

Отражают структуру БД

🔄 Бизнес-процессы
📌 Статусы дела

CREATED

IN_PROGRESS

COMPLETED

ARCHIVED

🔁 Разрешённые переходы
Текущий статус	Разрешённые переходы
CREATED	IN_PROGRESS, ARCHIVED
IN_PROGRESS	COMPLETED, ARCHIVED
COMPLETED	ARCHIVED
ARCHIVED	— (финальный статус)
🚫 Запрещённые переходы

COMPLETED → IN_PROGRESS

ARCHIVED → любой другой статус

CREATED → COMPLETED

При нарушении выбрасывается BusinessException.

📌 Обязательные условия

Нельзя завершить дело без документов

changedBy обязателен

Все изменения статуса записываются в StatusHistory

⚙️ Автоматические процессы

Scheduler ежедневно проверяет просроченные дела:

@Scheduled(cron = "0 0 1 * * ?")


Если дедлайн истёк:

Статус изменяется автоматически

В историю записывается "SYSTEM"

📚 API
Swagger
http://localhost:8089/swagger-ui.html

🔹 Создание дела

POST /cases

{
  "caseNumber": "CASE-001",
  "procedureType": "BANKRUPTCY"
}

🔹 Смена статуса

PATCH /cases/{id}/status

{
  "newStatus": "IN_PROGRESS",
  "changedBy": "admin"
}

🔹 Получение истории

GET /cases/{id}/history?page=0&size=10

🔹 Фильтрация по статусу

GET /cases?status=IN_PROGRESS&page=0&size=10
