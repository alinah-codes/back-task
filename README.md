Case Management System - 
Backend-приложение для управления делами (Cases) с поддержкой:
1. статусной машины
2. истории изменений
3. автоматических процессов


🔧 1. Запуск проекта
📌 Требования -
1. Java 17+
2. Maven 3.9+
3. PostgreSQL 14+
4. Spring Boot 3+


⚙️ Переменные окружения
Пример application.yml:
spring:
datasource:
url: jdbc:postgresql://localhost:5432/case_db
username: root
password: test1234

jpa:
hibernate:
ddl-auto: validate
show-sql: true
properties:
hibernate:
format_sql: true


🚀 Команды запуска
mvn clean install
mvn spring-boot:run


Приложение будет доступно:
http://localhost:8089

Swagger UI:
http://localhost:8089/swagger-ui.html


📚 2. Архитектура
Проект построен по слоистой архитектуре:
controller → service → repository → database


🧩 Слои приложения
Controller - 
1. Обрабатывает HTTP-запросы
2. Использует DTO
3. Не содержит бизнес-логики (тонкий слой)

Service - 

1. Содержит бизнес-логику
2. Управляет статусной машиной
3. Валидирует переходы статусов
4. Записывает историю изменений
5. Работает в транзакциях

Repository -
1. Доступ к данным через Spring Data JPA
2. Изолирован от бизнес-логики

Entity - 
1. JPA-сущности
2. Отражают структуру базы данных


⚙️ Как устроены процессы
1. Статусы реализованы через enum
2. Переходы валидируются на уровне Service
3. Все изменения записываются в StatusHistory
4. Автоматические изменения выполняются через @Scheduled

Exception - Ошибки бизнес-логики выбрасываются через BusinessException. 
Релизована глобальная обработка ошибок GlobalControllerAdvice

🔄 3. Бизнес-процессы
📌 Статусы дела
1. CREATED
2. IN_PROGRESS
3. COMPLETED
4. ARCHIVED

🔁 Разрешённые переходы
Текущий статус	Разрешённые переходы
CREATED	IN_PROGRESS, ARCHIVED
IN_PROGRESS	COMPLETED, ARCHIVED
COMPLETED	ARCHIVED
ARCHIVED	— (финальный статус)


🚫 Запрещённые переходы
COMPLETED → IN_PROGRESS
ARCHIVED → любой другой статус
CREATED → COMPLETED

При нарушении выбрасывается BusinessException.

📌 Обязательные условия
Нельзя завершить дело без документов
changedBy обязателен
Все изменения статуса записываются в StatusHistory

🤖 Автоматические процессы
Scheduler ежедневно проверяет просроченные дела:
@Scheduled(cron = "0 0 1 * * ?")

Если дедлайн истёк:
Статус изменяется автоматически
В историю записывается "SYSTEM"

📌 4. API
Swagger
http://localhost:8089/swagger-ui.html

🔹 Создание дела
POST /cases
{
"caseNumber": "CASE-001",
"procedureType": "BANKRUPTCY"
}

🔹 Смена статуса
PATCH /cases/{id}/status
{
"newStatus": "IN_PROGRESS",
"changedBy": "admin"
}

🔹 Получение истории
GET /cases/{id}/history?page=0&size=10


🔹 Фильтрация по статусу
GET cases?status=CREATED&page=0&size=10